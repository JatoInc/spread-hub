version: "3"

services:
  api_gateway:
    image: spread-hub:latest
    ports:
      - 3000:3000      
    deploy:
      replicas: 1
      update_config:
        parallelism: 1
        delay: 5s
    networks:
      - web

networks:
  web:
    driver: overlay
